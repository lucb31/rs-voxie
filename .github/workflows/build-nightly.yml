name: Nightly Build (Windows)

on:
  push:
    branches: ["main", "feat/pong_networked"]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build pong-client
        run: cargo build --release --target x86_64-pc-windows-msvc --bin pong-client --features="gui"

      - name: Build voxie
        run: cargo build --release --target x86_64-pc-windows-msvc --bin voxie --features="gui"

      - name: Prepare artifact directory
        run: |
          mkdir -p artifact
          cp target/x86_64-pc-windows-msvc/release/voxie.exe artifact/
          cp target/x86_64-pc-windows-msvc/release/pong-client.exe artifact/
          cp -r assets artifact/assets

      - name: Upload nightly artifact
        uses: actions/upload-artifact@v4
        with:
          name: rs-voxie-windows-nightly
          path: artifact
